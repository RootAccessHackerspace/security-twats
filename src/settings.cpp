#include "settings.h"

namespace Settings {
    // Define and initialize the variables here.  No 'constexpr' here.
    const bool lightsEnabled = true;
    const bool soundEnabled = true;
    const bool pushNotificationsEnabled = true;
}